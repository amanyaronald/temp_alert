meta {
  name: index
  type: http
  seq: 2
}

get {
  url: {{base_url}}/room-users?options[user_id]=1
  body: json
  auth: inherit
}

params:query {
  options[user_id]: 1
}

headers {
  Accept: application/json
}
